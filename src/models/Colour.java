package models;

public enum Colour {
    UNKNOWN,
    BLACK,
    WHITE
}

// style guide
// for every enum have a default enum value
// multiple good companies have a guide
// principles for good code in their code base
